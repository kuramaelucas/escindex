{
  "name": "esc",
  "private": true,
  "description": "Esc — plataforma de estudos para residência médica. Não precisa de instalação para rodar; este arquivo existe só para os testes automáticos.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 testes/*.test.mjs",
    "conferir": "node testes/conferir-dados.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
